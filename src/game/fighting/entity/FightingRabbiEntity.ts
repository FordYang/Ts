import FightingHeroEntity from "./FightingHeroEntity";

/**
 * 法师
 */
export default class FightingRabbiEntity extends FightingHeroEntity
{
    
    // /** 基础攻击力 */
    // public get baseAtk():number
    // {
    //     return this.attr.lucky ? this.attr.maxfs : this.attr.minfs + (Math.random() * (this.attr.maxfs - this.attr.minfs));
    // }
}