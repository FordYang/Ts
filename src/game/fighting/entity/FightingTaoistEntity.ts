import FightingHeroEntity from "./FightingHeroEntity";

/**
 * 道士
 */
export default class FightingTaoistEntity extends FightingHeroEntity
{

    // /** 基础攻击力 */
    // public get baseAtk():number
    // {
    //     return this.attr.lucky ? this.attr.maxds : this.attr.minds + (Math.random() * (this.attr.maxds - this.attr.minds));
    // }
}