import FightingHeroEntity from "./FightingHeroEntity";

/** 战士 */
export default class FightingSoliderEntity extends FightingHeroEntity
{
    
    // /** 基础攻击力 */
    // public get baseAtk():number
    // {
    //     return this.attr.lucky ? this.attr.maxzs : this.attr.minzs + (Math.random() * (this.attr.maxzs - this.attr.minzs));
    // }
}